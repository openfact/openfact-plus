<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd"
             version="2.1">
    <persistence-unit name="SyncPU" transaction-type="JTA">
        <jta-data-source>java:jboss/datasources/SyncDS</jta-data-source>
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
        <exclude-unlisted-classes>false</exclude-unlisted-classes>
        <properties>
            <!--Hibernate upgrade-->
            <property name="jboss.as.jpa.providerModule" value="org.hibernate:${version.hibernate}"/>
            <property name="wildfly.jpa.hibernate.search.module"
                      value="org.hibernate.search.orm:${version.hibernate-search}"/>

            <!--Hibernate properties-->
            <property name="hibernate.hbm2ddl.auto" value="create-drop"/>
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>

            <!--Hibernate Second Level Cache-->
            <property name="hibernate.cache.use_second_level_cache" value="true"/>

            <!--Hibernate search-->
            <!--<property name="hibernate.search.default.indexmanager" value="elasticsearch"/>-->
            <!--<property name="hibernate.search.default.elasticsearch.host" value="${es.host}"/>-->
            <!--<property name="hibernate.search.default.elasticsearch.username" value="${es.username}"/>-->
            <!--<property name="hibernate.search.default.elasticsearch.password" value="${es.password}"/>-->
            <!--<property name="hibernate.search.default.elasticsearch.required_index_status" value="yellow"/>-->
        </properties>
    </persistence-unit>
</persistence>
