{
  "realm": "${sso.realm:openfact}",
  "auth-server-url": "${sso.auth.server.url:http://localhost:8180/auth}",
  "resource": "${sso.clientId:openfact-online-platform}",
  "ssl-required": "external",
  "credentials": {
    "secret": "${sso.credentials.secret:openfact}"
  },
  "policy-enforcer": {}
}